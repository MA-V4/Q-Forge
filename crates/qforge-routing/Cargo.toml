[package]
name    = "qforge-routing"
version = "0.1.0"
edition = "2021"
description = "Hardware topology, qubit allocation, SWAP insertion, scheduling"

[dependencies]
qforge-ir  = { path = "../qforge-ir" }
serde      = { workspace = true }
serde_json = { workspace = true }
thiserror  = { workspace = true }
anyhow     = { workspace = true }