[package]
name    = "qforge-ir"
version = "0.1.0"
edition = "2021"
description = "Quantum IR — gate types, circuit representation, qubit references"

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
thiserror  = { workspace = true }
anyhow     = { workspace = true }
